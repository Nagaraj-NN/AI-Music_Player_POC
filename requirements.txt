# Core deep learning frameworks (CPU-only for 16GB RAM - saves 2GB+ vs CUDA)
torch==2.1.0  # CPU version recommended
torchaudio==2.1.0

# HuggingFace (minimal - dataset streaming to save RAM)
datasets==2.14.5
transformers==4.33.0  # ~1.5GB installed, Wav2Vec2 ~360MB cached

# Audio processing (lightweight alternatives)
librosa==0.10.1  # ~200MB - essential for audio features
soundfile==0.12.1  # ~50MB - audio I/O only

# API framework (lightweight)
fastapi==0.103.1  # ~50MB
uvicorn==0.23.2  # ~20MB
python-multipart==0.0.6  # ~10MB

# Data handling (core only, no pandas to save RAM)
numpy==1.24.3  # ~100MB - essential
scikit-learn==1.3.0  # ~200MB - train/val split only

# Model export (CPU inference only)
onnx==1.14.1  # ~50MB
onnxruntime==1.15.1  # ~200MB CPU version

# Spotify integration (Week 3 - optional for now)
spotipy==2.23.0  # ~10MB

# Visualization (optional - comment out if not needed)
# matplotlib==3.7.2  # ~300MB - only for debugging

# REMOVED heavy packages:
# - accelerate (not needed for CPU training)
# - pandas (numpy is sufficient)
# - pyaudio (will use soundfile for file-based audio)
# - CUDA/GPU pytorch (saves 2-3GB)

# Total estimated RAM usage during training: ~4GB peak
# Free RAM on 16GB laptop: ~12GB headroom
